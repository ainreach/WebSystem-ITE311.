# Disable directory browsing
Options -Indexes

# Redirect all requests to the public directory
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ITE311-NABALE/
    
    # Redirect to public directory
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>